import { motion, useScroll, useTransform } from "framer-motion";
import React, { useRef } from "react";
import './Initial.css'; // Import the CSS file

export default function Initial() {
  return (
    <div className="corner">
        JN
    </div>
  );
}